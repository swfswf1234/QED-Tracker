[core]
data_root = "data"
proxy = ""
timeout_seconds = 30
retries = 3
tls_verify = true
sources = [
  "internet_archive",
  "open_library",
  "google_books",
]

[axiom]
url = "http://127.0.0.1:8000"

[llm]
model = "qwen-plus"
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
timeout_seconds = 60
call_budget = 6
max_tokens = 4096
