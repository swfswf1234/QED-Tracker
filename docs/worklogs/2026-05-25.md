# 2026-05-25 Worklog

## T-201 数学教程一键补缺

### Changed

- Added strict one-click textbook collection to `app/collectors/textbook_hunter.py`.
- Extended `TextbookTarget` with `edition`, `kind`, and `required`.
- Added `--one-click`, `--missing-only`, and `--report` to `scripts/hunt_textbooks.py`.
- Added `scripts/probe_textbook_sources.py` and API-backed probes for Open Library, Internet Archive, and Google Books.
- Updated `docs/design/book_hunter_sources.md` and `docs/trackers/todos.md`.

### Verification

- `python3 -m py_compile app/curricula/base.py app/curricula/math_qe.py app/collectors/textbook_hunter.py app/tools/open_textbook_sources.py scripts/hunt_textbooks.py scripts/probe_textbook_sources.py tests/test_textbook_one_click.py tests/test_cli.py`
- Full pytest verification could not run in this shell because `pytest` is not installed and `conda`/`QED_env` is not available on PATH.
