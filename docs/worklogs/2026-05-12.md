# Worklog: 2026-05-12

## 操作内容
全面重建 QED-Tracker：精简范围，聚焦检索与分类。

## 变更清单

### 数据集
- 创建 `dataset/` 目录（gitignored）
- 按课程编号建立 10 个教材子目录 + papers/ + official_docs/
- 已有文件归位：`数学分析原理` + 吉米多维奇习题集 → `01_math_analysis/`
- 已有文件归位：`线性代数应该这样学` + 苏联习题集 → `02_linear_algebra/`

### 代码清理（删除）
| 模块 | 原因 |
|------|------|
| `src/engine/` (MinerU) | Part 2 的内容，本项目不需要 |
| `src/tracker/intelligence/` | 智能分析是后续阶段 |
| `app/repository/` (Qdrant) | 本项目不需要向量库 |
| `app/services/news_client.py` | 暂不追踪新闻 |
| `app/services/hackernews_client.py` | 暂不需要 |
| `app/services/report_generator.py` | 暂不需要每日简报 |
| 旧 models (user/project/news/material/user_label) | 数据库重建为 4 表 |

### 数据库模型（新建）
- `Textbook` — 教材位置索引（课程/书名/PDF路径/习题路径）
- `Paper` — 论文索引（arXiv ID/元数据/路径/课程标签）
- `OfficialDoc` — 官方文档索引（名称/版本/路径）
- `Resource` — 链接类资源（blog/video/article/project，仅存 PG）

### 采集器骨架（新建）
- `app/collectors/textbook_hunter.py` — 教材检索（一次性）
- `app/collectors/paper_collector.py` — arXiv 论文检索
- `app/collectors/doc_scraper.py` — 官方文档 HTML 爬取

### CLI 入口（新建）
- `scripts/hunt_textbooks.py` — 教材检索
- `scripts/hunt_papers.py` — 论文检索
- `scripts/hunt_docs.py` — 文档爬取
- `scripts/scan_dataset.py` — 扫描已有文件

### 配置
- `setting.ini` 精简为必需项
- `setting.example.ini` 新建
- `.gitignore` 添加 dataset/ 和 setting.ini
- `app/core/config.py` 重写

### 文档（全部重建）
- README.md / architecture.md / database.md
- design/math_qe_curriculum.md / book_hunter_sources.md
- trackers/todos.md / resolved.md
- knowledge_base/inventory.md / dependency_graph.md

## 验证
- [x] `python scripts/scan_dataset.py` 正确列出已有教材
- [x] `python -c "from app.core.config import settings; print(settings.dataset_path)"` 路径正确
- [x] `uvicorn app.main:app` 可启动

---

## 后续更新: Phase 1+2 教材检索访问方案 (2026-05-12 23:30)

### 问题
LibGen 原镜像 `libgen.is/rs/st` 已全部宕机，Clash 代理 SSL 握手失败导致 python-httpx 无法连接。

### 解决 (Phase 1 + 2)

| 文件 | 变更 |
|------|------|
| `app/collectors/textbook_hunter.py` | 重写 `LibGenHunter`: 镜像列表更新为 li/vg/la/bz/gl/gs/lc, SSL 上下文改用 `PROTOCOL_TLS_CLIENT`, search URL 改用 `index.php`, 解析兼容 9/10 列两种表格格式; 新增 `AnnaArchiveHunter` 类; `TextbookHunter` 集成双源 fallback |
| `app/core/config.py` | 新增 `http_proxy` 配置字段 |
| `setting.ini` | 添加 `[Proxy]` 段落 |
| `scripts/hunt_textbooks.py` | 适配新的 `TextbookHunter` API |
| `docs/design/book_hunter_sources.md` | 更新镜像列表 |
| `docs/trackers/todos.md` | T-102 移至已解决 |

### 当前状态
- LibGen 搜索恢复: `libgen.li/vg/la/bz/gl` 均可用 (25 rows/search)
- SSL 通过 `PROTOCOL_TLS_CLIENT` + `CERT_NONE` 上下文修复
- 下载通过 `file.php?id=` 端点进行
- Annas Archive `.gl` 可达但解析待完善
- 数据库 (PostgreSQL) 未启动, 需手动启动服务 + 运行 `init_db.py --create-db`
