# 2026-05-20 Worklog

## 文档体系整理

- 将 `docs/agents_read.md` 改名为 `docs/agent_protocol.md`。
- 重写 Agent 协议，明确阅读顺序、P-E-V-L 工作流、文档目录职责、Knowledge Core 规则、运行环境与 Git 检查项。
- 重写 `docs/design/README.md`，将设计文档分为 Domain Designs、Workflow Guides 和 System-Level Designs，并补充写作规则与新增文档规则。
- 更新 `README.md` 的文档导航，加入 `agent_protocol.md`、`design/README.md`、`tests.md` 和 `frontier_tracking.md`。

## 关键文件

| 文件 | 变更 |
| --- | --- |
| `docs/agent_protocol.md` | 新建正式 Agent 协议入口 |
| `docs/agents_read.md` | 删除旧临时入口 |
| `docs/design/README.md` | 重建设计文档体系说明 |
| `README.md` | 更新文档导航 |
| `docs/worklogs/2026-05-20.md` | 新建本记录 |

## 验证

- `rg -n "agents_read|agent_protocol|docs/design/README|design/README" README.md docs`：活跃入口已指向 `agent_protocol.md`；旧 `agents_read.md` 仅保留在历史 worklog 记录中。
- 检查 `README.md` 与 `docs/design/README.md`：新增导航条目均指向现有文档。

## Resources Hub v0.3

- 新增 `docs/design/resources_hub.md`，定义 resources 表统一资源中心、资源类型、CLI 合约、GitHub Collector 合约和测试策略。
- 新增实施计划 `docs/superpowers/plans/2026-05-20-resources-hub.md`。
- 扩展 `app/repository/resource_repo.py`：`get_by_url`、`exists_by_url`、`search`、`list_favorites`、`set_favorite`。
- 新增 `scripts/manage_resources.py`：支持 `list`、`search`、`favorite`、`export --format markdown`。
- 新增 `app/collectors/github_collector.py`：用 `GitHubDownloader.get_repo_info()` 将 GitHub 仓库元数据写入 `resources`。
- 新增 `scripts/hunt_github.py`：支持通过命令行参数或文本文件采集 GitHub repo 元数据。
- 新增测试文件 `tests/test_resource_repo.py`、`tests/test_github_collector.py`，并扩展 `tests/test_cli.py`。

### Resources Hub 验证

- `python -m pytest ...`：WSL 中 `python` 命令不存在。
- `python3 -m pytest tests/test_resource_repo.py -v`：当前 WSL 系统 Python 缺少 pytest。
- `python3` 依赖探测：当前 WSL 系统 Python 缺少 `sqlalchemy` 等项目依赖。
- `python3 -m py_compile app/repository/resource_repo.py app/collectors/github_collector.py scripts/manage_resources.py scripts/hunt_github.py tests/test_resource_repo.py tests/test_github_collector.py tests/test_cli.py`：语法检查通过。
- 结论：测试已按 TDD 写入，但当前 WSL shell 未连接到项目 Conda 环境 `QED_env`，需要激活 Windows/Conda 环境或在 WSL 内安装依赖后再运行 pytest。
