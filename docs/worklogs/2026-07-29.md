# 2026-07-29 工作日志

## 分支发布流程

### 变更

- 建立长期分支 `dev` 与 `release`，均从当前稳定的 `main` 创建并推送到远端。
- 更新 `docs/agent_protocol.md` 的 Git 规则：大改动按需使用 `feat/*`，日常集成进入 `dev`，发布候选从 `dev` 合并到 `release`，验收后再合并到 `main`。
- 明确发布验证期间在 `release` 修复的问题，随发布合并 `main` 后必须同步回 `dev`。

### 核验

- 确认创建前工作区干净，且本地和远端均不存在 `dev`、`release`。
- 确认新建分支与当前 `main` 初始指向同一提交，并设置对应的远端跟踪关系。
- 搜索分支规则，确认文档不再保留与该流程冲突的说明。

## QED-Tracker 0.3 聚焦重构

### 变更

- 将项目重建为可安装的 `qed-tracker` CLI，只保留教材/习题集、arXiv 论文下载、本地清单和 Axiom-Flow 显式交付。
- 建立六个教材来源适配器、arXiv 适配器、统一候选模型、严格目录匹配、Range 续传、PDF 校验、SHA-256 去重和原子落盘。
- 将 13 门数学课程的 44 个目标冻结为 `math-qe` JSON 目录；运行时不再依赖硬编码课程 Python 模块。
- 资源事实写入 `.qed-tracker/resources/<sha256>.json`，汇总 JSONL 可重建，Axiom transfer 状态独立保存。
- 增加 Axiom 健康检查、幂等上传和可选 `--parse` 页码范围；默认 push 不创建模型任务。
- 删除 0.2 的数据库、FastAPI、repository/model、GitHub/RSS/官方文档采集和旧脚本入口，重写当前架构与操作文档。
- 添加 Python 3.12 CI、标准 wheel 构建和完全离线的来源/下载/清单/CLI/Axiom 测试。

### 核验

- `python -m pytest tests -q`：27 passed。
- `python -m ruff check src tests`：通过。
- `python -m pip wheel . --no-deps --wheel-dir build/final-wheel`：成功生成 `qed_tracker-0.3.0-py3-none-any.whl`，并确认包含 `catalogs/math-qe.json`。
- 已安装命令验证：`qed-tracker --version`、`qed-tracker --json catalog list`、`qed-tracker config show` 通过。
- `git diff --check`：通过。
- 未扫描、移动或修改 `E:/qed/dataset` 中现有 PDF；仅创建了被 Git 忽略的本地配置。
