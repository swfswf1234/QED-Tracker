# 后续规划

状态：Current
最后更新：2026-09-11

本文件只记录能力方向，不记录任务状态、负责人或完成勾选。实际变更以 Git 分支、提交和测试结果为准。

## 下一阶段主线（待立项）

- **本地 LLM 模型替换**：评估以本地部署模型替换当前 API 调用的可行性与接入方式。
- **论文探索**：扩展论文发现与选择链路的探索能力。

## 服务化与统一接入

- 8901 服务接口（后台任务 + 轮询）与统一 CLI `qed` 承接后，评估任务并发、进度上报与前端展示
  的扩展（见 [ADR 0001](../adr/0001-tracker-service-architecture.md) 与
  [服务管理中心设计](../design/service-management.md)）。
- 重复下载链路与幂等行为已验证通过（QED-011，2026-09-11）；后续按真实使用反馈完善。

## 数据契约

- 领域/课程/教程确认端点校验并写回 `docs/knowledge/` 标准答案 JSON（manual@v1 契约，
  ADR 0006 决定 6 的阶段二「确认时写 JSON」；QED-053 关闭时未实施）——实施前需单独评审
  契约与端点改动、幂等规则与 @user 流程。

## 来源可靠性

- 为外部来源页面变化建立独立的人工连通性检查，结果不进入默认离线 CI。
- 复核来源授权、下载能力和可用性元数据，优先评估新的开放教材来源。
- 保持单个来源故障隔离，避免外部页面变化影响其他来源和本地清单。

## 目录与批处理

- 按现有 JSON schema 增加独立教材目录，不修改下载核心。
- 根据真实使用反馈完善批量恢复、失败报告和重复下载处理。

## Axiom-Flow 交付

- 在不共享数据库或数据目录的前提下完善传输状态查询。
- 继续保持默认只上传、显式解析和费用操作不自动重试的边界。

## 论文发现

- 建立人工标注的论文候选评测集，持续评估不同目标档案和模型版本的推荐精度。
- 根据真实使用反馈评估引用数据、周期跟踪和选择反馈；未经独立设计不得进入资源事实或自动下载链路。
