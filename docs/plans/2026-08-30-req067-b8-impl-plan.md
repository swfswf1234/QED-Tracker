# 领域探索状态接管（REQ-067 B8）实施计划

状态：Current
最后更新：2026-08-30

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 由 QED-Tracker（8901）接管领域探索的启动/执行/落库/名称确认，`exploration_stage` 五态（未开始/探索中/已生成/已完成/失败）单写方=8901，新增 `POST /domains/{id}/explore`、`POST /domains/{id}/confirm-name` 与 `qed_domain.explore_pending` 列，apply 全量自动落库。

**Architecture:** 复用现有 FastAPI + TaskManager（meta/tasks 持久化）+ KnowledgeRepository + prompt_lab `DomainPipeline`。探索触发由端点同步置「探索中」后提交 `domain_explore` 后台任务；任务在 `application/domain_explore.py` 的 `run_domain_explore` 内调用管线、按结果写「已生成/已完成/失败」终态与 `explore_pending`；名称确认端点仅在「已生成」态放行并重跑管线（`confirm_name_override`），成功后更新 domain 名并置「已完成」。导入链路（`POST /domains/import`）不动，保持「已完成」语义。

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy (MySQL 生产 / SQLite 测试), Alembic 迁移, prompt_lab DomainPipeline, TaskManager 后台任务。测试用 QED_env（conda env，`D:\software\anaconda3\envs\QED_env\python.exe`，PYTHONPATH=src）。

**关联：** 设计稿 `docs/plans/2026-08-30-req067-b8-explore-orchestration.md`（QED-051）；回执 `docs/plans/2026-08-30-req067-a-import-api-reply.md`（A 档，不在本计划改动）。

---

## 文件结构

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `src/qed_tracker/migrations/versions/0015_add_domain_explore_pending.py` | Create | qed_domain 加 `explore_pending JSON NULL` 列 |
| `src/qed_tracker/db/models.py` | Modify | QedDomain 加 `explore_pending` 字段 |
| `src/qed_tracker/db/knowledge_repository.py` | Modify | `update_domain` 支持 `name`/`explore_pending` |
| `src/qed_tracker/application/domain_explore.py` | Create | 探索状态机 + apply 全量落库（纯逻辑，可注入测试） |
| `src/qed_tracker/api/main.py` | Modify | 注册 `domain_explore` handler + explore/confirm-name 端点 + 视图透出 explore_pending |
| `docs/design/shared-tables.md` | Modify | qed_domain 写主体 8900→8901、8900 领域直写例外删除 |
| `docs/architecture/api.md` | Modify | 新增两端点契约 |
| `docs/architecture/database-schema.md` | Modify | explore_pending 列 + 失败态 |
| `docs/plans/index.md` | Modify | 登记本实施计划 |
| `tests/test_explore_ownership.py` | Create | 状态机/端点契约/apply/handler 四类测试 |

---

## Task 1: 迁移 0015 加 explore_pending 列

**Files:**
- Create: `src/qed_tracker/migrations/versions/0015_add_domain_explore_pending.py`
- Test: `tests/test_migration_0015.py`

- [ ] **Step 1: 写迁移模块（ASCII-only，注释走 tools/data）**

```python
"""Add explore_pending JSON column to qed_domain.

2026-08-30 REQ-067 B8: domain exploration state machine (explore/confirm-name)
writes pending name-confirmation or failure diagnostics here; read by GET
/domains and /courses for the frontend name-confirm / failure UX.

Chinese comments live in migrations/data/table_comments.json; this file stays
ASCII-only (Alembic reads migration modules with locale encoding on Windows/GBK).
"""

from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0015_add_domain_explore_pending"
down_revision = "0014_rebuild_qt_sources"
branch_labels = None
depends_on = None

_TABLE = "qed_domain"


def upgrade() -> None:
    op.add_column(_TABLE, sa.Column("explore_pending", sa.JSON(), nullable=True))


def downgrade() -> None:
    op.drop_column(_TABLE, "explore_pending")
```

- [ ] **Step 2: 写迁移测试**

```python
"""0015_add_domain_explore_pending 迁移定向测试（离线 SQLite）。"""

from __future__ import annotations

import importlib.util
from pathlib import Path

from alembic.migration import MigrationContext
from alembic.operations import Operations
from sqlalchemy import create_engine, inspect, text
from sqlalchemy.engine import Engine

MIGRATION_PATH = (
    Path(__file__).resolve().parents[1]
    / "src" / "qed_tracker" / "migrations" / "versions" / "0015_add_domain_explore_pending.py"
)


def _load_upgrade():
    spec = importlib.util.spec_from_file_location("migration_0015", MIGRATION_PATH)
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module.upgrade


def _upgrade(engine: Engine) -> None:
    upgrade = _load_upgrade()
    with engine.connect() as conn:
        ctx = MigrationContext.configure(conn)
        with Operations.context(ctx):
            upgrade()


def _columns(engine: Engine, table: str) -> set[str]:
    return {c["name"] for c in inspect(engine).get_columns(table)}


def test_adds_explore_pending_column(tmp_path):
    engine = create_engine(f"sqlite:///{tmp_path / 'a.db'}")
    with engine.begin() as conn:
        conn.execute(text("CREATE TABLE qed_domain (domain_id VARCHAR(32) PRIMARY KEY)"))
    _upgrade(engine)
    assert "explore_pending" in _columns(engine, "qed_domain")


def test_migration_module_is_ascii_only():
    content = MIGRATION_PATH.read_text(encoding="utf-8")
    content.encode("ascii")
```

- [ ] **Step 3: 运行测试（应 FAIL，迁移模块尚未创建）**

Run: `$env:PYTHONPATH="src"; & "D:\software\anaconda3\envs\QED_env\python.exe" -m pytest tests/test_migration_0015.py -q`
Expected: FAIL（模块不存在 / 断言失败）

- [ ] **Step 4: 创建迁移模块（Step 1 代码）**

Run: 用 Write 工具写入 `src/qed_tracker/migrations/versions/0015_add_domain_explore_pending.py`

- [ ] **Step 5: 运行测试（应 PASS）**

Run: `$env:PYTHONPATH="src"; & "D:\software\anaconda3\envs\QED_env\python.exe" -m pytest tests/test_migration_0015.py -q`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/qed_tracker/migrations/versions/0015_add_domain_explore_pending.py tests/test_migration_0015.py
git commit -m "feat(QED-051): add qed_domain.explore_pending migration 0015"
```

---

## Task 2: 模型字段 + repo 支持 name/explore_pending

**Files:**
- Modify: `src/qed_tracker/db/models.py:49-76`（QedDomain）
- Modify: `src/qed_tracker/db/knowledge_repository.py:207-235`（update_domain）

- [ ] **Step 1: QedDomain 加字段（models.py，`path_results` 之后）**

```python
    path_results: Mapped[dict | None] = mapped_column(JSON, nullable=True, comment="学习流程")
    explore_pending: Mapped[dict | None] = mapped_column(JSON, nullable=True, comment="探索挂起信息（名称确认/失败诊断）")
```

- [ ] **Step 2: update_domain 支持 name + explore_pending（knowledge_repository.py）**

把签名与函数体改为（其余分支保留）：

```python
    def update_domain(self, domain_id: str, *, name: str | None = None,
                      description: str | None = None, stages: list[str] | None = None,
                      level: str | None = None, scope: str | None = None,
                      classic_tracks: list[dict[str, Any]] | None = None,
                      path_results: dict[str, Any] | None = None,
                      exploration_stage: str | None = None,
                      explore_pending: dict[str, Any] | None = None) -> QedDomain:
        """更新领域（name 仅探索名确认路径可写；path_results/exploration_stage/explore_pending
        属探索产物，本仓库写方）。"""
        with self._session_factory() as session:
            row = session.get(QedDomain, domain_id)
            if row is None:
                raise KeyError(f"领域不存在：{domain_id}")
            if name is not None:
                row.name = name
            if description is not None:
                row.description = description
            if stages is not None:
                row.stages = stages
            if level is not None:
                row.level = level
            if scope is not None:
                row.scope = scope
            if classic_tracks is not None:
                row.classic_tracks = classic_tracks
            if path_results is not None:
                row.path_results = path_results
            if exploration_stage is not None:
                row.exploration_stage = exploration_stage
            if explore_pending is not None:
                row.explore_pending = explore_pending
            row.updated_at = utc_now()
            session.commit()
            session.refresh(row)
            return row
```

- [ ] **Step 3: 写 repo/模型测试（追加到 test_db_models.py 或独立）**

在 `tests/test_db_models.py` 追加：

```python
def test_update_domain_supports_name_and_explore_pending(tmp_path):
    from sqlalchemy import create_engine
    from sqlalchemy.orm import sessionmaker
    from qed_tracker.db.knowledge_repository import KnowledgeRepository
    from qed_tracker.db.models import Base, QedDomain
    from qed_tracker.database import utc_now

    engine = create_engine(f"sqlite:///{tmp_path / 'd.db'}")
    Base.metadata.create_all(engine)
    factory = sessionmaker(bind=engine, expire_on_commit=False)
    session = factory()
    now = utc_now()
    session.add(QedDomain(domain_id="math", name="数学", description="d", stages=["本科基础"],
                          created_at=now, updated_at=now))
    session.commit()
    repo = KnowledgeRepository(lambda: factory())
    row = repo.update_domain("math", name="数学（高等数学）", explore_pending={"kind": "failed", "error": "x"})
    assert row.name == "数学（高等数学）"
    assert row.explore_pending == {"kind": "failed", "error": "x"}
```

- [ ] **Step 4: 运行测试（需先实现 Step 1/2）**

Run: `$env:PYTHONPATH="src"; & "D:\software\anaconda3\envs\QED_env\python.exe" -m pytest tests/test_db_models.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/qed_tracker/db/models.py src/qed_tracker/db/knowledge_repository.py tests/test_db_models.py
git commit -m "feat(QED-051): QedDomain.explore_pending + update_domain name/explore_pending"
```

---

## Task 3: 探索状态机核心（domain_explore.py）

**Files:**
- Create: `src/qed_tracker/application/domain_explore.py`
- Test: `tests/test_explore_ownership.py`

- [ ] **Step 1: 写核心纯逻辑（可注入 repo + pipeline，不触网）**

```python
"""领域探索状态机（QED-051 / REQ-067 B8）。

纯逻辑编排：接收 KnowledgeRepository 与 DomainPipeline（可注入假管线），
负责「探索中→已生成/已完成/失败」状态迁移、explore_pending 写负、全量自动落库。
不直接持有 FastAPI/网络；终态由调用方（api task handler）经 TaskManager 落盘。

与 prompt_lab dry-run（只读不写）不同：本模块是正式流程，会写共享表。
"""

from __future__ import annotations

from typing import Any

from qed_tracker.db.knowledge_repository import KnowledgeRepository
from qed_tracker.prompt_lab.pipeline import NameConfirmationRequired

_STAGES = ["基础", "主干", "分支", "前沿"]


def _apply_domain(repo: KnowledgeRepository, domain_id: str, report: dict[str, Any]) -> None:
    """领域字段 upsert（探索产物列 by 本仓库；name 由确认路径单独写）。"""
    d = report["domain"]
    path = report.get("path", {})
    repo.update_domain(
        domain_id,
        description=d.get("description", ""),
        level=d.get("level", ""),
        classic_tracks=d.get("classic_tracks", []),
        stages=list(_STAGES),
        path_results={"edges": path.get("edges", []), "graph_td": path.get("graph_td", "")},
    )


def _apply_courses(repo: KnowledgeRepository, domain_id: str, report: dict[str, Any]) -> dict[str, int]:
    """课程幂等 upsert（course_id=slug，契约同 import）；返回 {created, updated}。"""
    created = 0
    updated = 0
    for index, course in enumerate(report.get("courses", [])):
        slug = str(course["slug"])
        fields = dict(
            domain_id=domain_id, name=course["name"], stage=course.get("stage", ""),
            sort_order=index, description=course.get("summary", ""),
            aliases=course.get("aliases", []), track=course.get("track", ""),
            prerequisites=course.get("prerequisites", []),
        )
        if repo.get_course(slug) is None:
            repo.create_course(course_id=slug, **fields)
            created += 1
        else:
            repo.update_course(slug, stage=fields["stage"], sort_order=fields["sort_order"],
                               description=fields["description"], aliases=fields["aliases"],
                               track=fields["track"], prerequisites=fields["prerequisites"])
            updated += 1
    return {"created": created, "updated": updated}


def run_domain_explore(repo: KnowledgeRepository, pipeline: Any, *, domain_id: str,
                       scope_hint: str = "", mode: str = "direct", ref_text: str = "",
                       ref_doc_path: str = "", confirm_name_override: str = "") -> dict[str, Any]:
    """执行一次领域探索，返回 outcome 与落库统计。

    状态语义：
    - 调用方须已置「探索中」（explore/confirm-name 端点同步置位）；
    - NameConfirmationRequired → 置「已生成」+ explore_pending(name_confirm)，返回 outcome=confirmation_required；
    - 成功 → apply 全量落库 → 置「已完成」，explore_pending=None；若有 confirm_name_override 且改名，写 name；
    - 其他异常 → 置「失败」+ explore_pending(failed)，重抛（交给 TaskManager 记 failed）。
    """
    domain = repo.get_domain(domain_id)
    if domain is None:
        raise KeyError(f"领域不存在：{domain_id}")
    try:
        report = pipeline.explore(
            domain.name,
            scope_hint=scope_hint, mode=mode, ref_text=ref_text, ref_doc_path=ref_doc_path,
            confirm_name_override=confirm_name_override,
        )
    except NameConfirmationRequired as exc:
        repo.update_domain(domain_id, exploration_stage="已生成",
                           explore_pending={"kind": "name_confirm", "name_check": exc.name_check})
        return {"outcome": "confirmation_required", "domain_id": domain_id}

    final_name = (confirm_name_override or "").strip() or str(domain.name)
    if confirm_name_override and final_name != domain.name:
        repo.update_domain(domain_id, name=final_name)

    _apply_domain(repo, domain_id, report)
    counts = _apply_courses(repo, domain_id, report)
    repo.update_domain(domain_id, exploration_stage="已完成", explore_pending=None)
    return {"outcome": "applied", "domain_id": domain_id,
            "courses_created": counts["created"], "courses_updated": counts["updated"]}
```

- [ ] **Step 2: 写核心单测（fake pipeline，离线）**

```python
"""领域探索状态机测试（QED-051）：run_domain_explore 终态 + apply 全量落库。"""

from __future__ import annotations

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from qed_tracker.application.domain_explore import run_domain_explore
from qed_tracker.db.knowledge_repository import KnowledgeRepository
from qed_tracker.db.models import Base, QedDomain
from qed_tracker.database import utc_now
from qed_tracker.prompt_lab.pipeline import NameConfirmationRequired, PipelineError


@pytest.fixture
def repo(tmp_path):
    engine = create_engine(f"sqlite:///{tmp_path / 'e.db'}")
    Base.metadata.create_all(engine)
    factory = sessionmaker(bind=engine, expire_on_commit=False)
    session = factory()
    now = utc_now()
    session.add(QedDomain(domain_id="math", name="数学", description="d", stages=["本科基础"],
                          created_at=now, updated_at=now))
    session.commit()
    yield KnowledgeRepository(lambda: factory())
    engine.dispose()


class OkPipeline:
    def explore(self, domain_name, *, scope_hint="", mode="direct", ref_text="", ref_doc_path="",
                confirm_name_override=""):
        return {
            "domain": {"final_name": confirm_name_override or domain_name, "description": "新描述",
                       "level": "本科", "classic_tracks": [{"name": "分析学", "kind": "main"}]},
            "courses": [
                {"slug": "analysis", "name": "数学分析", "stage": "基础", "summary": "s",
                 "track": "分析学", "aliases": [], "prerequisites": []},
            ],
            "path": {"edges": [], "graph_td": "graph TD\n"},
        }

    def close(self):
        return None


def test_success_applies_and_sets_completed(repo):
    result = run_domain_explore(repo, OkPipeline(), domain_id="math")
    assert result["outcome"] == "applied"
    domain = repo.get_domain("math")
    assert domain.exploration_stage == "已完成"
    assert domain.explore_pending is None
    assert domain.description == "新描述"
    assert domain.path_results == {"edges": [], "graph_td": "graph TD\n"}
    assert repo.get_course("analysis") is not None


def test_confirmation_required_sets_generated(repo):
    class ConfirmPipeline(OkPipeline):
        def explore(self, *args, **kwargs):
            raise NameConfirmationRequired({"valid": False, "suggested_name": "高等数学", "reason": "r"})

    result = run_domain_explore(repo, ConfirmPipeline(), domain_id="math", confirm_name_override="")
    assert result["outcome"] == "confirmation_required"
    domain = repo.get_domain("math")
    assert domain.exploration_stage == "已生成"
    assert domain.explore_pending["kind"] == "name_confirm"
    assert domain.explore_pending["name_check"]["suggested_name"] == "高等数学"


def test_failure_sets_failed_and_reraises(repo):
    class BoomPipeline(OkPipeline):
        def explore(self, *args, **kwargs):
            raise PipelineError("boom", code="INVALID_PARAMS")

    with pytest.raises(PipelineError):
        run_domain_explore(repo, BoomPipeline(), domain_id="math")
    domain = repo.get_domain("math")
    assert domain.exploration_stage == "失败"
    assert domain.explore_pending["kind"] == "failed"


def test_confirm_override_renames(repo):
    result = run_domain_explore(repo, OkPipeline(), domain_id="math", confirm_name_override="高等数学")
    assert result["outcome"] == "applied"
    domain = repo.get_domain("math")
    assert domain.name == "高等数学"
    assert domain.exploration_stage == "已完成"
```

- [ ] **Step 3: 运行（应 FAIL：模块未创建）**

Run: `$env:PYTHONPATH="src"; & "D:\software\anaconda3\envs\QED_env\python.exe" -m pytest tests/test_explore_ownership.py -q`
Expected: FAIL（ModuleNotFoundError / 断言失败）

- [ ] **Step 4: 实现模块（Step 1 代码）**

- [ ] **Step 5: 运行（应 PASS）**

- [ ] **Step 6: Commit**

```bash
git add src/qed_tracker/application/domain_explore.py tests/test_explore_ownership.py
git commit -m "feat(QED-051): domain exploration state machine + full apply"
```

---

## Task 4: API 端点 + 视图 + handler 注册

**Files:**
- Modify: `src/qed_tracker/api/main.py`（handler 注册、explore/confirm-name 端点、`_domain_view`/`_domain_view_flat`）

- [ ] **Step 1: 顶部 import 加 run_domain_explore + DomainExploreError 相关确认**

在 `from qed_tracker.application.knowledge_import import KnowledgeImportError, validate_domain` 之后加：

```python
from qed_tracker.application.domain_explore import run_domain_explore
```

（`_advisor_kwargs`、`DomainPipeline`、`NameConfirmationRequired` 已在文件顶部 import。）

- [ ] **Step 2: 在 all_handlers 处注册 domain_explore handler（`_book_download_handler` 之后）**

```python
    def _domain_explore_handler(params: dict[str, Any], progress) -> dict[str, Any]:
        """领域探索后台任务：管线执行 → 状态机终态 + 全量落库（REQ-067 B8）。"""
        repo = _kn(app)
        pipeline = DomainPipeline(**{**_advisor_kwargs()})
        try:
            return run_domain_explore(
                repo, pipeline,
                domain_id=str(params["domain_id"]),
                scope_hint=str(params.get("scope_hint", "")),
                mode=str(params.get("mode", "direct")),
                ref_text=str(params.get("ref_text", "")),
                ref_doc_path=str(params.get("ref_doc_path", "")),
                confirm_name_override=str(params.get("confirm_name_override", "")),
            )
        finally:
            pipeline.close()
```

并把注册改为：

```python
    all_handlers = {**(extra_handlers or {}), "book_download": _book_download_handler,
                    "domain_explore": _domain_explore_handler}
```

- [ ] **Step 3: 在领域维护端点区（`create_domain` 之前）加两个端点**

```python
    def _require_explore_param(mode: str) -> str:
        if mode not in ("direct", "text", "doc"):
            raise api_error(400, "INVALID_PARAMS", "mode 必须为 direct/text/doc")
        return mode

    @fastapi_app.post("/api/v1/domains/{domain_id}/explore", status_code=202)
    def explore_domain(domain_id: str, payload: dict[str, Any] = _EMPTY_BODY) -> dict[str, str]:
        """启动领域探索（REQ-067 B2/B8）：同步置「探索中」→ 提交 domain_explore 后台任务。"""
        repo = _kn(app)
        if repo.get_domain(domain_id) is None:
            raise api_error(404, "DOMAIN_NOT_FOUND", f"领域不存在：{domain_id}")
        mode = _require_explore_param(str(payload.get("mode", "direct") or "direct"))
        if repo.get_domain(domain_id).exploration_stage == "探索中":
            raise api_error(409, "DOMAIN_EXPLORING", f"领域正在探索中：{domain_id}")
        repo.update_domain(domain_id, exploration_stage="探索中", explore_pending=None)
        record = manager.submit("domain_explore", {
            "domain_id": domain_id, "mode": mode,
            "scope_hint": str(payload.get("scope_hint", "")),
            "ref_text": str(payload.get("ref_text", "")),
            "ref_doc_path": str(payload.get("ref_doc_path", "")),
        })
        return {"task_id": record.task_id}

    @fastapi_app.post("/api/v1/domains/{domain_id}/confirm-name", status_code=202)
    def confirm_domain_name(domain_id: str, payload: dict[str, Any] = _EMPTY_BODY) -> dict[str, str]:
        """确认领域名称（REQ-067 B7）：仅「已生成」态放行 → 重跑管线 → 已完成。"""
        repo = _kn(app)
        domain = repo.get_domain(domain_id)
        if domain is None:
            raise api_error(404, "DOMAIN_NOT_FOUND", f"领域不存在：{domain_id}")
        if domain.exploration_stage != "已生成":
            raise api_error(409, "INVALID_TRANSITION", f"仅「已生成」态可确认名称：{domain.exploration_stage}")
        decision = str(payload.get("decision", "")).strip()
        if decision not in ("accept", "custom", "retain"):
            raise api_error(422, "INVALID_PARAMS", "decision 必须为 accept/custom/retain")
        name = str(payload.get("name", "")).strip()
        pending = (domain.explore_pending or {}).get("name_check", {}) if domain.explore_pending else {}
        suggested = str(pending.get("suggested_name", "")).strip()
        if decision == "accept":
            final_name = name or suggested or domain.name
            if not final_name or len(final_name) > 100:
                raise api_error(422, "INVALID_PARAMS", "最终名称非空且 ≤100")
        elif decision == "custom":
            if not name or len(name) > 100:
                raise api_error(422, "INVALID_PARAMS", "custom 必须提供 name（非空且 ≤100）")
            final_name = name
        else:  # retain
            final_name = domain.name
        repo.update_domain(domain_id, exploration_stage="探索中", explore_pending=None)
        record = manager.submit("domain_explore", {
            "domain_id": domain_id, "mode": "direct",
            "confirm_name_override": final_name,
        })
        return {"task_id": record.task_id}
```

- [ ] **Step 4: 视图透出 explore_pending（`_domain_view` 与 `_domain_view_flat`）**

在 `_domain_view` 的返回 dict（`path_results` 后）加：

```python
            "explore_pending": domain.explore_pending,
```

在 `_domain_view_flat` 的返回 dict（`path_results` 后）加：

```python
            "explore_pending": domain.explore_pending,
```

- [ ] **Step 5: 写端点契约测试（追加到 test_explore_ownership.py，用 repo client fixture）**

```python
# --- API 端点契约（TestClient + 注入 SQLite repo；探索任务用假 handler 离线） ---

import time

from fastapi.testclient import TestClient
from qed_tracker.api.main import create_app
from qed_tracker.config import load_settings


@pytest.fixture
def api(repo):
    app = create_app(load_settings(data_root=repo.__dict__.get("_path", ".")), knowledge_repository=repo)
    return TestClient(app)
```

由于 create_app 需要 tmp_path 的 data_root，且 handler 会执行真管线，我们改为注入 `extra_handlers` 替换 domain_explore。追加：

```python
def _fake_explore_handler(params, progress):
    return {"outcome": "applied", "domain_id": params["domain_id"],
            "courses_created": 0, "courses_updated": 0}


@pytest.fixture
def api(tmp_path, repo):
    app = create_app(load_settings(data_root=tmp_path), knowledge_repository=repo,
                     extra_handlers={"domain_explore": _fake_explore_handler})
    return TestClient(app)


def _wait(client: TestClient, task_id: str, timeout: float = 5.0) -> dict:
    deadline = time.time() + timeout
    while time.time() < deadline:
        resp = client.get(f"/api/v1/tasks/{task_id}")
        assert resp.status_code == 200
        data = resp.json()
        if data["status"] in ("succeeded", "failed"):
            return data
        time.sleep(0.02)
    raise AssertionError("task 未结束")


def test_explore_sets_exploring_and_submits(api, repo):
    resp = api.post("/api/v1/domains/math/explore", json={})
    assert resp.status_code == 202
    assert "task_id" in resp.json()
    assert repo.get_domain("math").exploration_stage == "探索中"
    _wait(api, resp.json()["task_id"])


def test_explore_409_when_running(api, repo):
    repo.update_domain("math", exploration_stage="探索中")
    resp = api.post("/api/v1/domains/math/explore", json={})
    assert resp.status_code == 409
    assert resp.json()["detail"]["code"] == "DOMAIN_EXPLORING"


def test_explore_404_unknown_domain(api, repo):
    resp = api.post("/api/v1/domains/nope/explore", json={})
    assert resp.status_code == 404


def test_explore_rejects_bad_mode(api, repo):
    resp = api.post("/api/v1/domains/math/explore", json={"mode": "web"})
    assert resp.status_code == 400


def test_confirm_name_exploring_gets_409(api, repo):
    repo.update_domain("math", exploration_stage="探索中")
    resp = api.post("/api/v1/domains/math/confirm-name", json={"decision": "accept"})
    assert resp.status_code == 409
    assert resp.json()["detail"]["code"] == "INVALID_TRANSITION"


def test_confirm_name_accept_without_name_uses_suggested(api, repo):
    repo.update_domain("math", exploration_stage="已生成",
                       explore_pending={"kind": "name_confirm",
                                        "name_check": {"suggested_name": "高等数学", "valid": False, "reason": "r"}})
    resp = api.post("/api/v1/domains/math/confirm-name", json={"decision": "accept"})
    assert resp.status_code == 202
    # 假 handler 不落库; 端点已置「探索中」
    assert repo.get_domain("math").exploration_stage == "探索中"


def test_confirm_name_custom_requires_name(api, repo):
    repo.update_domain("math", exploration_stage="已生成")
    resp = api.post("/api/v1/domains/math/confirm-name", json={"decision": "custom"})
    assert resp.status_code == 422


def test_domains_view_exposes_explore_pending(api, repo):
    repo.update_domain("math", exploration_stage="失败",
                       explore_pending={"kind": "failed", "error": "boom"})
    resp = api.get("/api/v1/domains")
    row = next(d for d in resp.json() if d["domain_id"] == "math")
    assert row["explore_pending"] == {"kind": "failed", "error": "boom"}
```

> 注意：Step 2 里 `_domain_explore_handler` 用了 `DomainPipeline`，测试通过 `extra_handlers` 替换后不会触网；但 dry-run/真实 handler 仍需在 QED-Tracker 侧由 monkeypatch 保护（见 Task 5）。

- [ ] **Step 6: 运行端点测试（需先实现 Step 3/4/2，应 PASS）**

Run: `$env:PYTHONPATH="src"; & "D:\software\anaconda3\envs\QED_env\python.exe" -m pytest tests/test_explore_ownership.py -q`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add src/qed_tracker/api/main.py tests/test_explore_ownership.py
git commit -m "feat(QED-051): explore/confirm-name endpoints + domain_explore handler + view expose"
```

---

## Task 5: 真 handler 探针测试（monkeypatch 假管线，验证任务链路）

**Files:**
- Test: `tests/test_explore_ownership.py`（追加一块，用 monkeypatch DomainPipeline）

- [ ] **Step 1: 追加真 handler 测试（monkeypatch api_main.DomainPipeline 为假管线，不注册 extra_handlers）**

```python
def test_task_handler_runs_success_offline(tmp_path, repo, monkeypatch):
    from qed_tracker.api import main as api_main

    class FakePipeline:
        def __init__(self, **kwargs):
            self.kwargs = kwargs
        def explore(self, *args, **kwargs):
            return {
                "domain": {"final_name": "数学", "description": "d", "level": "本科",
                           "classic_tracks": []},
                "courses": [],
                "path": {"edges": [], "graph_td": ""},
            }
        def close(self):
            self.closed = True

    monkeypatch.setattr(api_main, "DomainPipeline", FakePipeline)
    app = api_main.create_app(load_settings(data_root=tmp_path), knowledge_repository=repo)
    with TestClient(app) as client:
        resp = client.post("/api/v1/domains/math/explore", json={})
        assert resp.status_code == 202
        data = _wait(client, resp.json()["task_id"])
    assert data["status"] == "succeeded"
    assert data["result"]["outcome"] == "applied"
    assert repo.get_domain("math").exploration_stage == "已完成"
```

> 说明：`_domain_explore_handler` 内 `DomainPipeline(**{**_advisor_kwargs()})` 在无 DB 配置时 `engine=None`，llm_client 落 qed_llm_calls 静默降级；FakePipeline 忽略 kwargs。测试全程离线。

- [ ] **Step 2: 运行（应 PASS）**

- [ ] **Step 3: Commit（并入上次 commit，或单独）**

```bash
git add tests/test_explore_ownership.py
git commit -m "test(QED-051): domain_explore task handler offline probe"
```

---

## Task 6: 文档同步 + 台账 + 全量门禁

**Files:**
- Modify: `docs/design/shared-tables.md`（写主体口径修订）
- Modify: `docs/architecture/api.md`（端点契约）
- Modify: `docs/architecture/database-schema.md`（explore_pending 列 + 失败态）
- Modify: `docs/plans/index.md`（登记本实施计划）
- Test: `tests/test_documentation.py` 新增文档无需白名单（已在设计稿登记？本实施计划若列入 plans/index 需同步白名单）

- [ ] **Step 1: shared-tables.md 修订 qed_domain 写主体**

找到 `exploration_stage` 状态机表（qed_domain 部分）与写权限例外表，把 qed_domain 行改为：

```
| 探索中 | 领域探索会话启动 | **8901** | 本仓库端点同步置位（REQ-067 B8 修订） |
| 已生成 | 名称需确认 | **8901** | 管线除名称确认外完成，explore_pending=name_confirm |
| 已完成（领域） | 探索应用完成 | **8901** | apply 全量落库（REQ-067 B8 修订） |
| 失败 | 探索异常 | **8901** | explore_pending=failed（REQ-067 B8 新增） |
```

写权限例外表：删除 qed_domain 行（8900 直写例外），保留 qed_course 行。

- [ ] **Step 2: database-schema.md 同步**

- qed_domain 字段表加 `explore_pending JSON NULL`；
- `exploration_stage` 枚举补「失败」，写主体注明 QED-Tracker；
- 状态机迁移合法性注：非法迁移 409（INVALID_TRANSITION / DOMAIN_EXPLORING）。

- [ ] **Step 3: api.md 同步**

在领域维护端点区加两个端点契约（路径/请求/响应/错误码），对齐 main.py 实现。

- [ ] **Step 4: plans/index.md 登记本实施计划**

追加一行：

```
- [领域探索状态接管（REQ-067 B8）实施计划](2026-08-30-req067-b8-impl-plan.md)（2026-08-30，QED-051）：设计稿的落地实施（迁移 0015 / 状态机 / 端点 / 文档同步）。
```

- [ ] **Step 5: 运行全部门禁**

Run:
```bash
$env:PYTHONPATH="src"; & "D:\software\anaconda3\envs\QED_env\python.exe" -m pytest -q
& "D:\software\anaconda3\envs\QED_env\python.exe" -m ruff check .
$env:PYTHONPATH="src"; & "D:\software\anaconda3\envs\QED_env\python.exe" -m pytest tests/test_documentation.py -q
```
Expected: 新增测试全绿；既有全量不回归（注意工作区已有未提交改动，以不与用户改动冲突为准）；test_documentation 中 `test_all_documentation_links_resolve` 若有预存断链（根仓库引用的 `2026-08-exploration-download-flow.md`、`docs/standards/documentation.md`）属既有失败，不在本计划修复。

- [ ] **Step 6: Commit**

```bash
git add docs/design/shared-tables.md docs/architecture/api.md docs/architecture/database-schema.md docs/plans/index.md
git commit -m "docs(QED-051): sync shared-tables/api/database-schema + register impl plan"
```

---

## Self-Review

- **Spec 覆盖：** 设计稿 §3 状态机（五态+409）→ Task 3/4 覆盖；§4 端点（explore/confirm-name + 视图透出）→ Task 4；§5 handler（engine=app._db_engine、budget 隔离、重跑）→ Task 4/5；§6 apply 全量落库（domain 四列 + courses upsert + path_results）→ Task 3；§7 契约修订 → Task 6；§8 回执清单（8900/前端）→ 交由 QED-Engine 通知（不在本仓库改动）；§9 测试计划 → Task 3/4/5；迁移 0015 → Task 1。
- **Placeholder scan：** 无 TBD；每步含可执行代码与命令。
- **Type consistency：** `run_domain_explore`（Task3）签名与 Task4/5 调用一致（`domain_id/scope_hint/mode/ref_text/ref_doc_path/confirm_name_override`）；`repo.update_domain(name=..., explore_pending=...)`（Task2）与 Task3/4 一致；`_domain_view`/`_domain_view_flat` 均加 `explore_pending`。
